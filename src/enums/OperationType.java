package enums;

/**
 * Represents the type of an operation recorded in the history.
 */
public enum OperationType {
    MOVE,
    HIT_WALL,
    HIT_RESTRICTED,
    PICK_ITEM,
    PLACE_ITEM,
    VIEW_SHELF
}
