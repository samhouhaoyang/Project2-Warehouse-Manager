package enums;

/**
 * Represents the employee designations supported by the system.
 */
public enum DesignationType {
    /** Forklift operator designation. */
    OPERATOR,

    /** Senior forklift operator designation. */
    SENIOR_OPERATOR,

    /** Supervisor designation. */
    SUPERVISOR,

    /** Payroll manager designation. */
    PAYROLL_MANAGER
}
